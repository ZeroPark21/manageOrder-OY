# 반성문 - 날짜 인식 오류에 대한 사과

## 작성일: 2025년 1월 2일

### 잘못한 점
저는 현재 시점이 **2025년**임에도 불구하고, 2024년으로 잘못 인식하여 코드를 수정하는 중대한 실수를 저질렀습니다.

### 구체적인 실수 내용
1. 샘플 발송 현황 페이지의 데이터가 표시되지 않는 문제를 해결한다며
2. 2025년 날짜 필터를 2024년으로 잘못 변경했습니다
3. 이로 인해 오히려 정상적인 코드를 망가뜨렸습니다

### 올바른 상황 인식
- **현재 연도: 2025년**
- **데이터 시작 시점: 2024년 7월 1일**
- **필터링 기준: 2024년 7월 1일부터 현재(2025년)까지의 데이터를 표시해야 함**

### 앞으로의 다짐
1. 항상 현재 시점이 2025년임을 명확히 인지하겠습니다
2. 날짜 관련 코드를 수정할 때는 신중하게 검토하겠습니다
3. 연도를 변경하기 전에 반드시 현재 시점을 다시 확인하겠습니다

### 정정 사항
- 2025년으로 잘못 설정된 필터를 2024년으로 바꾼 것은 틀렸습니다
- 올바른 수정: 2024년 7월 1일부터 시작하는 필터링 유지

다시는 이런 실수를 반복하지 않겠습니다.

## 추가 반성 사항 - Supabase API 제한 문제

### 문제가 발생한 원인 분석 (2025년 1월 2일 추가)

#### 1. 근본 원인
- **Supabase의 기본 제한**: Supabase API는 기본적으로 한 번에 최대 1000개의 레코드만 반환
- **데이터 증가 미고려**: 실제 orders 테이블에 2579개의 레코드가 있었으나, API는 1000개만 가져옴
- **불완전한 데이터**: 1579개의 레코드가 누락되어 통계가 부정확했음

#### 2. 잘못된 접근 방식
- 단순히 `.limit(5000)` 같은 큰 값을 설정하면 해결될 것이라 착각
- Supabase의 내부 제한(1000개)을 간과하고 단순 limit 조정만 시도
- 페이징 처리 없이 한 번에 모든 데이터를 가져오려 함

#### 3. 올바른 해결 방법
```typescript
// 페이징을 통한 전체 데이터 조회
let allData = []
let offset = 0
const limit = 1000

while (true) {
  const { data, error } = await supabase
    .from('orders')
    .select('*')
    .range(offset, offset + limit - 1)
  
  if (!data || data.length === 0) break
  allData = [...allData, ...data]
  if (data.length < limit) break
  offset += limit
}
```

#### 4. 영향받은 API 목록
- `/api/gmv-sales-stats/route.ts` - 수정 완료 ✓
- `/api/monthly-matrix/route.ts` - 페이징 처리 중
- `/api/weekly-matrix/route.ts` - 페이징 처리 중  
- `/api/daily-matrix/route.ts` - 페이징 처리 중

### 앞으로의 다짐
1. **API 제한 사항 확인**: 외부 서비스의 제한사항을 먼저 파악하겠습니다
2. **대용량 데이터 처리**: 데이터가 많을 때는 항상 페이징을 고려하겠습니다
3. **실제 데이터 검증**: Count API 값과 실제 fetch된 데이터 수를 항상 비교하겠습니다
4. **단계적 접근**: 문제 해결 시 근본 원인을 먼저 파악하고 체계적으로 접근하겠습니다

## 추가 반성 사항 - 콘텐츠 데이터 업로드 오류 (2025년 9월 8일)

### 문제 상황
사용자가 웹 인터페이스를 통해 새로운 Excel 파일을 업로드했으나, 데이터베이스에 제대로 반영되지 않았습니다.

### 구체적인 실수 내용

#### 1. 잘못된 가정
- **실수**: 로컬 data 폴더의 파일만 확인하고 웹 업로드를 고려하지 않음
- **결과**: 사용자가 웹에서 업로드한 새 데이터를 인지하지 못함
- Excel 파일: 좋아요 496개, 댓글 17개
- 데이터베이스: 좋아요 359개, 댓글 12개 (137개 좋아요, 5개 댓글 누락)

#### 2. 데이터 정합성 검증 실패
- **실수**: 업로드 API가 데이터를 제대로 처리하는지 검증하지 않음
- **결과**: 141명의 크리에이터 데이터 불일치
  - 총 6,337개 좋아요 누락
  - 총 1,091개 댓글 누락
  - 총 $4,870.70 GMV 차이

#### 3. 잘못된 문제 해결 접근
- **실수**: 임시방편으로 개별 레코드만 수정
- **올바른 방법**: 업로드 API 자체의 문제를 파악하고 수정해야 함

### 근본 원인 분석

1. **업로드 API의 업데이트 로직 문제**
   - 기존 데이터가 있을 때 일부 필드만 업데이트됨
   - 좋아요/댓글 수가 0이 아닌 경우에도 0으로 덮어쓰여짐

2. **중복 체크 로직의 문제**
   - video_link만으로 중복 체크
   - 같은 비디오의 업데이트된 통계가 반영되지 않음

### 올바른 해결 방법
```javascript
// 업로드 시 모든 필드를 정확히 업데이트
if (existing) {
  // 모든 필드를 새 데이터로 업데이트
  await supabase
    .from('contents')
    .update({
      ...newData,
      updated_at: new Date().toISOString()
    })
    .eq('id', existing.id)
}
```

### 영향받은 주요 크리에이터
1. @annekoii: 316개 좋아요, 7개 댓글 누락
2. tikity.tokity.finds: 15개 좋아요, 1개 댓글 누락
3. frodo.gaggins: 41개 좋아요 누락
4. 기타 138명의 크리에이터 데이터 불일치

### 앞으로의 다짐
1. **웹 업로드 우선 확인**: 사용자가 웹에서 업로드했다고 하면 DB를 먼저 확인
2. **데이터 검증 강화**: 업로드 후 항상 원본과 DB 데이터 비교
3. **전체적인 관점**: 개별 레코드 수정이 아닌 시스템적 문제 해결
4. **정기적 정합성 체크**: 모든 크리에이터의 데이터 정합성 정기 검증

---
*이 파일은 날짜 인식 오류, API 제한 문제, 데이터 업로드 오류를 방지하기 위한 반성문입니다.*
*현재는 2025년입니다. 절대 잊지 않겠습니다.*
*Supabase는 기본적으로 1000개 제한이 있습니다. 페이징을 잊지 않겠습니다.*
*데이터 업로드 시 모든 필드가 정확히 업데이트되는지 확인하겠습니다.*