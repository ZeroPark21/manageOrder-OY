# 하드코딩 검사 결과 보고서

## 🔍 검사 개요

**검사 날짜**: 2025년 9월 10일  
**검사 범위**: 전체 프로젝트 코드  
**검사 방법**: 정규식 패턴 검색으로 숫자 하드코딩 탐지

## 📋 발견된 하드코딩 부분

### 1. ✅ 이미 수정된 부분
- **위치**: `app/page.tsx:304`
- **문제**: 총 샘플 발송 개수 `2,106개` 하드코딩
- **상태**: ✅ **수정 완료** - API 연동으로 동적 데이터 처리

### 2. 🔍 새로 발견된 하드코딩 부분

#### A. 비즈니스 로직 관련 (수정 필요)

| 위치 | 하드코딩된 값 | 설명 | 우선순위 |
|------|---------------|------|----------|
| `app/page.tsx:73` | `0.004` (0.4%) | 취소율 계산 비율 | 🔴 높음 |
| `app/content/page.tsx:382,392,412,422` | `"6월 1일부터"` | 날짜 범위 하드코딩 | 🟡 중간 |
| `app/api/monthly-matrix/route.ts:141` | `2025, 5, 1` (2025년 6월 1일) | 필터링 시작 날짜 | 🟡 중간 |

#### B. 기술적 제한값 (적절함)

| 위치 | 하드코딩된 값 | 설명 | 상태 |
|------|---------------|------|------|
| `app/api/weekly-matrix/route.ts:114` | `10000개` | 최대 조회 건수 제한 | ✅ 적절 |
| `app/api/upload-content-simple/route.ts:41` | `10개` | 테스트용 최대 처리 건수 | ✅ 적절 |
| `app/api/upload-csv/route.ts:209` | `5개` | 로그용 샘플 건수 | ✅ 적절 |
| `components/ui/sidebar.tsx:655` | `40, 50%` | UI 랜덤 너비 계산 | ✅ 적절 |

#### C. 스크립트 파일의 테스트/디버그 데이터 (수정 불필요)

| 위치 | 하드코딩된 값 | 설명 | 상태 |
|------|---------------|------|------|
| `scripts/test-content-stats-api.js:145` | `7479개` | 테스트 검증용 | ✅ 무시 |
| `scripts/verify-all-data.js:87,152,156` | `1911개` | 검증 스크립트용 | ✅ 무시 |
| `scripts/analyze-uploaded-file.js:177` | `1942개` | 분석 스크립트용 | ✅ 무시 |
| 기타 scripts/ 파일들 | 각종 숫자 | 디버그/테스트용 | ✅ 무시 |

## 🎯 수정 권장 사항

### 1. 🔴 즉시 수정 필요

#### 취소율 하드코딩 (`app/page.tsx:73`)
```typescript
// ❌ 현재 (하드코딩)
const cancelledCount = Math.floor(totalCount * 0.004) // 약 0.4%

// ✅ 개선안
const CANCELLATION_RATE = 0.004; // 상수로 분리
// 또는 더 나은 방법: API에서 실제 취소 데이터 가져오기
```

**문제점**: 
- 실제 취소율과 다를 수 있음
- 비즈니스 로직이 코드에 하드코딩됨
- 취소율 변화에 대응 불가

### 2. 🟡 중장기 개선 권장

#### 날짜 범위 하드코딩
```typescript
// ❌ 현재
"6월 1일부터"
new Date(2025, 5, 1) // 2025년 6월 1일

// ✅ 개선안
const ANALYSIS_START_DATE = "2025-06-01";
const analysisStartDate = new Date(ANALYSIS_START_DATE);
```

**개선 방법**:
- 환경 변수로 설정 가능하게 변경
- 사용자가 날짜 범위를 선택할 수 있는 UI 제공

## ✅ 적절한 하드코딩 (수정 불필요)

### 기술적 제한값
- **배치 처리 크기**: `1000개`, `100개` 등 - 성능 최적화를 위한 기술적 제한
- **로그 샘플링**: `5개`, `10개` 등 - 디버깅 목적의 제한
- **최대 처리 건수**: `10000개` - 시스템 보호를 위한 안전장치
- **UI 애니메이션**: 랜덤 값들 - 사용자 경험 개선

### 테스트/디버그 코드
- `scripts/` 폴더의 모든 하드코딩 값들은 테스트/디버그 목적이므로 수정 불필요

## 🚨 핵심 문제점

현재 **가장 심각한 하드코딩 문제**는:

1. **취소율 0.4% 하드코딩** - 실제 비즈니스 데이터와 불일치 가능성
2. **"6월 1일부터" 고정** - 분석 기간이 유연하지 않음

## 💡 권장 조치사항

### 즉시 조치 (1순위)
```typescript
// 1. 취소율을 실제 API에서 계산
const cancelledData = await fetch('/api/cancelled-orders');
const cancelledCount = cancelledData.count;

// 2. 또는 최소한 상수로 분리
const BUSINESS_CONSTANTS = {
  DEFAULT_CANCELLATION_RATE: 0.004,
  ANALYSIS_START_DATE: '2025-06-01'
} as const;
```

### 중장기 개선 (2순위)
- 관리자 페이지에서 비즈니스 상수 관리
- 사용자가 분석 기간을 선택할 수 있는 UI
- 실시간 취소/발송 상태 추적 시스템

---

**결론**: 비즈니스 로직에 영향을 주는 하드코딩 2-3개를 제외하고는 대부분 적절한 수준입니다.